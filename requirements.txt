# Install these packages in a virtual environment
# python -m venv .venv
# .venv\Scripts\activate (Windows) or source .venv/bin/activate (Linux/Mac)
# pip install -r requirements.txt

Django==4.1.13  # Downgraded to be compatible with djongo
Pillow==11.1.0
pycryptodome==3.22.0
six==1.17.0
stripe==12.0.1
python-dotenv==1.1.0  # Required for .env file support
requests==2.32.3
dnspython==2.6.0  # For DNS lookups
gunicorn==23.0.0  # For production deployment
whitenoise==6.6.0  # For static files in production
# MongoDB support
djongo==1.3.6  # Django MongoDB connector
pymongo==3.12.3  # Required specific version for djongo
sqlparse==0.2.4  # Required by djongo
pytz==2023.3  # Required by djongo
# Cloudinary support
cloudinary==1.37.0 
django-cloudinary-storage==0.3.0
# Optional but recommended
jinja2==3.1.5  # For template rendering
# psycopg2-binary==2.9.10  # For PostgreSQL support (not needed with MongoDB)